# fast_bitrix24

Canonical GitHub repository: https://github.com/alexeyleshchenko/fast_bitrix24

Canonical Python package: `fast-bitrix24` on PyPI (https://pypi.org/project/fast-bitrix24/).

The old GitHub URL `https://github.com/leshchenko1979/fast_bitrix24` is a public 404. Do not restore that owner in README links, badges, `setup.py`, or docs. PyPI is the stable homepage pointer.

Do not create a replacement repository under `leshchenko1979/fast_bitrix24`. If GitHub ever unsuspends that account and allows a transfer, keep the old name free so redirects can work.
